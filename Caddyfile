:8000 {
    # Route all API requests to the api container
    handle /api/* {
        reverse_proxy api:8080
    }

    # Route all other requests to the webapp container
    handle {
        reverse_proxy webapp:5173 {
            header_up Host localhost
        }
    }
}
