# Nexa Platform v0.6.1 - AV2 Runtime Validation Patch

## Summary

Patch release for AV2 backend runtime validation.

## Changes

- Aligned project version metadata with the current AV2 patch release.
- Preserved the validated .NET 10 backend configuration.
- Kept local credential files excluded from version control.

## Validation

- `dotnet restore`
- `dotnet build`
- `dotnet ef database update --project King.Nexa.Platform/King.Nexa.Platform.csproj`
- Swagger and main GET endpoints validated locally.
- PostgreSQL count checks validated against `nexa_platform_db`.

## Notes

- Local database credentials must remain in ignored local configuration files.
